name: Smart Triggers

on:
  push:
    branches:
      - main
      - release/**

    paths:
      - 'src/**'
      - 'app/**'

jobs:
  run-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Code change detected in src or app"
